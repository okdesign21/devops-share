# Example .env for Terraform + Infisical integration
# Copy to .env and fill values. This file is ignored by git.

# Terraform variables for EKS secret creation
TF_VAR_infisical_client_id=
TF_VAR_infisical_client_secret=

# Infisical context for CLI fetches
INFISICAL_PROJECT_ID=

# Optional: pre-provide Cloudflare token (otherwise fetched via CLI when applying DNS)
# TF_VAR_cloudflare_api_token=
